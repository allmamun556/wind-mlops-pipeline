__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/
.streamlit/
mlartifacts/
# MLflow local run/artifact store — not a DVC output, grows unbounded with
# every retrain (one full model copy per run). mlflow.db keeps run metadata;
# the served model is models/best_model.pkl, tracked separately.
mlruns/
*.egg-info/
.DS_Store
venv/

# Large raw data is DVC-tracked, not committed directly to git history
# (uncomment once a DVC remote is configured):
# data/raw/turbine_data.csv
